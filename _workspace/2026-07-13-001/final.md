[BLOCK1 L93]
9년차 백엔드 개발자입니다. 데이터 흐름과 시스템 신뢰성 설계에 집중해왔고 증상만 막기보다 같은 문제가 다시 나오지 않는 구조를 만드는 쪽을 선호합니다. SaaS 메신저(잔디), 임대주거 플랫폼(에피소드), 군·공공 관제 솔루션을 거치며 결제 플로우와 동시성, 분산 캐시, 외부 API 장애 격리, 배치 적재 구조를 다뤘습니다. 기술 판단이 곧 제품 방향으로 이어지는 조직에서 더 큰 규모와 복잡한 도메인을 설계하는 역할을 맡고 싶습니다.

[BLOCK2 L132]
계약·재계약·퇴거·정산 모듈 전반을 처음부터 직접 설계하고 구현했습니다. 레거시 API와 신규 v2 API가 함께 돌아가는 구조라 JPA와 MyBatis를 섞어 썼고 상태 전이 과정에서 예기치 못한 부작용이 생기지 않도록 MyBatis 쿼리를 전부 직접 검수해 영향 범위를 짚었습니다. 정산 모듈은 퇴실예정 등록 → 만기해지/중도퇴거 분기 → 사용료 청구 → 보증금 상계 → 미납금·유보금 계산 → 입주자 확인 → 공실 전환까지 금융 플로우 전체를 설계·구현했습니다. 여러 담당자가 같은 계약을 동시에 수정하는 상황에 대비해 DB 유니크 제약으로 상태 중복을 1차로 막고 정산 확정처럼 정합성이 절대적인 구간은 비관적 락(SELECT FOR UPDATE)을, 조회가 잦은 구간은 낙관적 락(@Version)을 나눠 적용했습니다. eRoom API 기반 Python 마이그레이션 스크립트로 계약 데이터 27만 건을 옮겼고 대사 스케줄러가 미처리 건을 자동 재시도하고 DLQ로 이관하며 외부 응답과 비교 검증하도록 해 정합성 확보와 자동 복구를 한 메커니즘으로 묶었습니다. 핵토파이낸셜 가상계좌 인프라 구축과 연동을 맡아 가상계좌 발급 API와 입금 콜백 처리, 정산 트리거까지 결제 플로우 전 구간을 직접 구현했습니다. 외부 API 폭주를 막으려고 핵토 쓰로틀링, ALB rate limit, 1분 100건 chunk 이관까지 3중으로 방어했고 웹소켓 입금 신호가 누락되면 1분 주기 폴링 reconciliation으로 스스로 복구하는 구조를 설계했습니다.

[BLOCK3 L151]
삼성·LG·현대·아카라를 공통 추상화 계층으로 묶어 새 기기는 인터페이스 구현만으로 붙일 수 있는 구조를 설계했습니다. 아카라 도어락은 MQTT 브로커 로그를 분석해 구독 만료가 처리되지 않는다는 근본 원인을 찾았고 자동 재구독 로직을 넣어 정상 동작 비율을 600호실(40%)에서 1,500호실 전체로 끌어올렸습니다. LG·현대 신규 5개 사이트를 연동할 때 생긴 오프라인 감지 문제도 재갱신 로직으로 풀었습니다. 같은 장비에 동시 제어 요청이 몰려도 서로 부딪치지 않도록 Redisson 분산 락을 걸어 정합성을 보장했습니다.

[BLOCK4 L169]
반복되는 사내 업무 조회·기록을 자연어 한 번으로 처리하자는 아이디어를 직접 제안해 정식 프로젝트로 개발했습니다. 자연어 요청을 인텐트로 분해한 뒤 Datadog·Jira·Confluence·MySQL·Notion 5개 사내 시스템 커넥터로 곧바로 조회하도록 설계했고 MCP 커넥터로는 단순 조회를 넘어 Jira 이슈 생성이나 Confluence 페이지 작성 같은 ‘행동’까지 하도록 확장했습니다. 사용자별 회의록·티켓·결정 사항은 로컬 임베딩 기반 하이브리드 RAG(키워드 + 벡터 검색)로 관련 내용만 추려 LLM에 넣어 외부 임베딩 비용 없이 개인화 메모리를 구현했습니다. 세션에 들어오면 오늘의 Jira 할일과 지난 세션 중단 지점, 일정을 자동으로 브리핑해 ‘어제 어디까지 했는지’ 되짚는 수고를 없앴습니다. 밖에서 익힌 도구를 사내 자산으로 바꾼 사례로, 회의록 요약과 관련 Jira 이슈, 최근 Datadog 에러 지표를 한 번에 받는 통합 응답을 운영하고 있습니다.

[BLOCK5 L243]
코드베이스 전체를 살펴보니 S3 SDK를 직접 호출하는 코드가 40개 이상 파일에 흩어져 있었고 Presigned URL·CDN URL·멀티파트 업로드처럼 벤더마다 구현이 달라지는 지점이 최소 3곳이었습니다. 단순 find-replace로는 부족하고 추상화 계층이 필요하다고 보고 Strategy 패턴으로 분리해 어떤 클라우드 벤더가 들어와도 구현체만 갈아 끼우면 전환되는 구조를 설계했습니다. 이를 Nexus에 배포해 팀 전체가 쓰는 내부 라이브러리로 공급했고 환경변수 스위치 하나로 AWS S3와 카카오 Object Storage를 바꿔 끼울 수 있게 했습니다. 덕분에 실제 전환도 서비스 코드 변경 없이 무중단으로 마쳤습니다.

[BLOCK6 L259]
서비스 간 호출 그래프를 분석해 보니 메시지·알림·검색 세 서비스가 동기 호출로 강하게 얽혀 있었고 한 서비스가 장애를 일으키면 나머지 두 곳까지 30초 안에 번지는 구조였습니다. 이벤트 기반으로 바꾸되 같은 사용자의 이벤트 순서가 꼬이면 데이터 정합성이 깨지므로 userId 해시 기반 8파티션으로 순서 보장과 병렬성을 함께 확보했고 Consumer Group 3개를 서비스별로 독립시켜 한 곳의 장애가 전체로 번지던 구조를 끊었습니다. 신규 서비스를 연동할 때도 기존 코드를 손대지 않고 Consumer Group만 추가하면 확장할 수 있는 구조로 설계했습니다. 설계와 배포를 마친 뒤 팀에 인계했습니다.

<!-- HUMANIZE-SUMMARY v1.6.1
run_id: 2026-07-13-001
metrics:
  char_in: 1930
  char_out: 1938
  change_rate: 18.7%  # 편집 비율 추정(글자수는 윤문 특성상 비슷)
  self_check: 6/6
  grade: A
categories:  # before → after
  C-11 연결어미 뒤 쉼표(-고/-며/-어 직후): 11 → 0
  A-1 '~에 대한/동일 N': 2 → 0
  A-11 '~을 위해' 목적절: 2 → 0
  D 동어 반복('전담...전담 구현'): 1 → 0
  E-1 리듬 균일(장문 분할): 2건 분할 적용
self_check:
  - 고유명사·수치·인용 100% 보존: OK
  - 변경률 30% 이하: OK (18.7%)
  - 장르 이탈 없음(이력서 공적): OK
  - register 보존(격식체 ~습니다): OK
  - S1 잔존 0건: OK
  - 인공 표현 추가 없음: OK
highlights:
  - id: D-dup
    before: "핵토파이낸셜 가상계좌 인프라 구축·연동을 전담했고, ... 결제 플로우 전 구간을 전담 구현했습니다."
    after: "핵토파이낸셜 가상계좌 인프라 구축과 연동을 맡아 ... 결제 플로우 전 구간을 직접 구현했습니다."
  - id: A-1
    before: "동일 장비에 대한 동시 제어 요청이 경합하지 않도록"
    after: "같은 장비에 동시 제어 요청이 몰려도 서로 부딪치지 않도록"
  - id: E-1
    before: "...구조를 기반으로 실제 전환을 서비스 코드 변경 없이 무중단으로 완료했습니다."
    after: "...바꿔 끼울 수 있게 했습니다. 덕분에 실제 전환도 서비스 코드 변경 없이 무중단으로 마쳤습니다."
residual_findings: (없음)
grade_reason: "A — S1 잔존 0건, 변경률 18.7%, 자체검증 6항 통과. 이력서 공적 register(격식체) 그대로 유지, 수치·고유명사 100% 보존, 창작 0건."
-->
